# bTool 用户手册

一个给 ESP32 系列板子用的图形化小工具。把平常要敲命令行的几件事收进界面：

| 能做什么 | 对应页签 |
|---|---|
| **烧写固件**（.bin） | 烧写 |
| **和板子对话** —— MicroPython REPL 交互 | REPL 终端 |
| **在电脑和板子之间传文件** | 文件管理 |

**芯片识别和烧写面向全部 ESP 系列** —— ESP32 / S2 / S3 / C2 / C3 / C5 / C6 / C61 /
H2 / H21 / H4 / P4 / E22，以及 ESP8266。不依赖板子上跑什么固件。

> 不熟 ESP32 也没关系 —— 烧写那部分照着做就行。REPL 那节解释了"和板子对话"是怎么回事。

---

## 一、准备

### 1. 装驱动

板子通过 USB 转串口芯片连电脑，**没装驱动就看不到串口**。常见两种：

| 芯片 | 出现在设备管理器里的名字 | 驱动 |
|---|---|---|
| CH343 / CH340 | `USB-Enhanced-SERIAL CH343` | 沁恒 WCH 官网 |
| CP210x | `Silicon Labs CP210x` | 乐鑫 / Silicon Labs 官网 |

判断装好没有：**把板子插上，工具顶部的「串口」下拉里能出现一个 COM 口**。

> ⚠ 用**能传数据的 USB 线**。有些线只能充电，插上去毫无反应 ——
> 换根线往往比折腾驱动快。

### 2. 启动工具

- **打包版**：直接双击 `bTool.exe`，不用装 Python。
- **源码版**：`pip install pyserial esptool`，然后 `python btool.py`。

---

## 二、连接板子

顶部那一条是连接栏：

```
串口: [COM14 (USB-SERIAL CH343) ▾]  [刷新]   芯片: ESP32-S3   语言: [中文 ▾]   [连接] [断开]
```

1. **插上板子**，等一两秒 —— 串口列表**会自己刷新**，不用点「刷新」。
   拔掉的板子也会自己从列表里消失。
2. **选串口**。认不出哪个是板子，就把别的 USB 串口设备拔了再看。
3. 点 **「连接」**。（芯片型号是工具自己读的，不用你选 —— 见下节）

连上后左下角状态栏会显示 `● 已连接 COM14 @115200`。

### 怎么知道这是什么芯片

**「芯片」那个框不用你填，也填不了** —— 型号是工具自己去芯片里读的，
读到了就显示，没读到就显示「未识别」。

读芯片有**两个时机**，都会在**烧写日志**里打出完整的硬件信息：

| 时机 | 怎么触发 | 会重启板子吗 |
|---|---|---|
| **烧写时** | 你点「擦除并烧写」 | 会（烧写本来就要重启）|
| **点「读芯片信息」按钮** | 你点它 | **会** —— 所以点之前会问你一句 |

打出来的东西长这样：

```
Chip type:          ESP32-S3 (QFN56) (revision v0.2)
Features:           Wi-Fi, BT 5 (LE), Dual Core + LP Core, 240MHz, Embedded PSRAM 8MB (AP_3v3)
Crystal frequency:  40MHz
MAC:                14:c1:9f:51:ed:28

[分区] nvs 24.0 K · phy_init 4.0 K · factory 7.9 M · vfs 8.0 M
```

### 关于「读芯片信息」这个按钮

**它什么板子都认** —— 跑 MicroPython 的、裸机烧了别的程序的、出厂一片空白的，
全都读得出来。因为它是跟芯片里**出厂自带的那段 ROM 程序**说话，
跟你往板子上刷了什么**完全无关**。

**代价是板子会被重启。** 要跟 ROM 说话，芯片必须先复位进「下载模式」；
读完再复位回来 —— 上面正在跑的程序会重新开始。所以：

- 它**不会自动跑**（不然每次连板子都把你程序重启一遍）
- 点下去会**先问你一句**，你确认了才动手

不想重启板子、只是想知道型号？—— 那就**直接烧**：烧写时反正要重启，
芯片信息会顺手打出来，不用多点一次。

### 分区表怎么看

| 分区 | 是干什么的 |
|---|---|
| `factory` | **程序区** —— 固件装在这儿。它满了就烧不进新固件 |
| `vfs` | **文件系统区** —— 你上传的文件放这儿 |
| `nvs` | 存掉电不丢的设置（标定值、配网信息）。**勾了「擦除整片 Flash」就会清掉它** |
| `phy_init` | 射频校准数据，固定 4 K |

> 只想烧固件的话，**可以不点「连接」**，直接去烧写页也行 ——
> 烧写时工具会自己去开串口。但连一下能提前发现"串口被别的程序占着"这类问题。

### 连不上怎么办

| 现象 | 处理 |
|---|---|
| 串口列表是空的 | 线不行 / 驱动没装 / 板子没供电 |
| 打开串口"拒绝访问" | 被别的程序占着 —— 关掉 ViperIDE、串口助手、另一个 bTool |
| 连上了但看不到 `>>>` | 板子在跑死循环程序，按 **`Ctrl+C`** 打断 |

---

## 三、烧写固件

切到 **「烧写」** 页。

### 步骤

1. 点 **「添加固件…」**（可多选）。地址会**按文件名自动填**：

   | 文件名里有 | 自动填的地址 |
   |---|---|
   | `bootloader` | `0x0` |
   | `partition-table` | `0x8000` |
   | `boot_app0` | `0xE000` |
   | 其它（通常就是主固件） | `0x10000` |

   这个规则覆盖了绝大多数 ESP-IDF 工程的布局。**地址不对就双击那一格手改**，
   用十六进制填（如 `0x10000`）。

2. 选 **烧写波特率**：`115200` 最稳，`921600` 最快。**正常情况下可以放心用 921600**。

3. **决定要不要勾「先擦除整片 Flash」** —— 见下面的警告，**默认别勾**。

4. 点 **「擦除并烧写」**。进度条在上面，下面是 esptool 的原始日志。

> 按钮叫「擦除并烧写」是沿用下来的名字 —— **没勾「先擦除」时它就是"开始烧写"**，
> 不会真的去擦。看到这个名字不用紧张。

### ⚠ 关于「先擦除整片 Flash」

> **擦除会清空整片 Flash，包括 NVS 里保存的标定 / 配置数据。**
>
> NVS 是板子上存"掉电不丢的设置"的地方 —— 很多固件把**舵机标定值、Wi-Fi 密码、
> 用户设置**放在那儿。擦了就全没了，得重新标定 / 重新配网。

| 什么时候勾 | 什么时候别勾 |
|---|---|
| 改过分区表 | **平时更新程序** |
| 板子状态不明、烧不进去 | |

绝大多数情况**直接烧、不勾擦除**就对了。

### 烧写时会发生什么

工具会先**断开自己的串口连接**（日志里打 `[已释放串口供烧写使用]`），
因为 esptool 要独占串口；烧完自动重连（`[串口已重新打开]`）。
如果这时正开着 REPL，终端会短暂断开一下，正常现象。

---

## 四、REPL 终端 —— 和板子对话

切到 **「REPL 终端」** 页。

### REPL 是什么

**REPL** = Read-Eval-Print Loop，读一句、算一句、回一句。跑 MicroPython 的板子上
有一个交互式命令行：**你敲一行，板子马上执行，并把结果显示回来**。

不用编译、不用烧写，适合：

- 试一段代码行不行
- 看板子当前状态（读传感器、看变量）
- 跑个小测试

> 这一页**不是**"输入框 + 输出框"，而是**一个终端** ——
> 直接在那个黑框里敲，就跟串口终端一样。

### 怎么用

1. **先连接**（见第二节）。连上后 REPL 里会出现 `>>>` 提示符。
2. **直接在黑框里打字**，字符会发到板子。按 **回车** 执行。
3. 板子执行的结果、报错，显示在下面。

```python
>>> print("hello")
hello
>>> 1 + 1
2
>>> import machine
>>> machine.freq()
240000000
```

> **屏幕上出现的字是板子回显回来的**，不是本地画上去的。
> 所以打完字要等板子回应才显示 —— 这是**真终端**的行为，
> 好处是"你看到的就是板子上真实发生的"。

### 按键

| 按键 | 作用 |
|---|---|
| 普通字符 | 原样发给板子 |
| `Enter` | 执行这一行 |
| `↑` `↓` | **翻历史命令**（记最近 8 条）|
| `←` `→` | **移动光标**，可以在行中间改字 |
| `Home` `End` | 跳到行首 / 行尾 |
| `Backspace` | 删掉光标前一个字符 |
| `Ctrl+C` | **看你有没有选中文字**：选中了 → **复制**；没选中 → **打断板子** |
| `Ctrl+Shift+C` / `Ctrl+Insert` | **复制**（不管有没有选中）|
| `Ctrl+V` / `Shift+Insert` | **粘贴**（把剪贴板内容发给板子）|
| `Ctrl+D` | 软复位板子 |
| **右键** | 弹出菜单：**复制 / 粘贴 / 全选 / 清屏** |

> **`Ctrl+C` 会"看情况"** —— 这是为了避免误伤：
> 终端里 `Ctrl+C` 天生是"打断"（往板子发一个字节 `0x03`，MicroPython 收到就中断当前程序），
> 不是复制。但大家的肌肉记忆都是"Ctrl+C = 复制"，想复制一段输出时
> 顺手就把板子上跑的程序打断了。所以这里按**有没有选中文字**分流：
>
> - **选中了** → 只复制，**不打断**
> - **没选中** → 打断（这时也没东西可复制，`Ctrl+C` 只能是打断）
>
> 想"无论如何都要复制"，用 `Ctrl+Shift+C`。想复制整屏，右键 → 全选。
>
> **为什么需要"打断"**：如果你烧进去的程序里有 `while True:`（比如一直跑的循环），
> 板子永远回不到 `>>>`，REPL 看起来像死了。这时按 `Ctrl+C` 把循环打断，提示符就回来了。

> **粘贴后最后一行会停在命令行上等你按回车** —— 跟手打的一样，你有机会看一眼再决定跑不跑。
>
> | 粘的内容 | 粘完是什么状态 |
> |---|---|
> | **单行** | 内容**打到了命令行上，没执行** —— 按回车才跑 |
> | **多行·简单语句** | 前面几行**已执行**，**最后一行停在命令行上等你回车** |
> | **多行·带缩进**（`for` / `if` / `def`…）| **整段直接执行**（这种没法只留最后一行）|
>
> **为什么前几行必须执行**：不执行就没法跟后面分开 —— 它们会黏成一行。
>
> **粘贴过的行都在历史里**（简单语句那种），以后按 `↑` 能翻出来。
> 带缩进的整段是走另一个通道的，**翻不到** —— 反复要用的代码建议存成板子上的
> `.py`，以后 `import` 一句就够了。
>
> 多行粘贴时可能看到板子回一句 `paste mode; Ctrl-C to cancel, Ctrl-D to finish`，
> 那是正常的，说明它进入了"整段接收"状态。

### 「切回 REPL」按钮

MicroPython 的 REPL 有**两种模式**：

| | 友好 REPL（普通）| Raw REPL |
|---|---|---|
| **给谁用** | **人** | **程序** |
| 回显 | 有（你打的字板子回显给你）| 没有 |
| 提示符 | `>>>` | 没有 |
| 输出格式 | 混着提示符和回显，给人看 | `OK<输出>\x04<错误>\x04>`，严格的机器协议 |

**这件事完全不用你管。** 做文件操作（列目录 / 上传 / 下载 / 删除 / 重命名）时，
工具会自动切到 Raw —— 那些操作要把板子的输出**一字不差**抓回来解析，
而友好 REPL 会把你的输入也回显一遍、还夹着 `>>>`，程序分不清哪段是结果。
做完工具会自动切回来。

**这个按钮平时是灰的**，你不用理它。它**只有一个用处**：万一终端卡在 Raw 模式
（那个模式下**键盘输入会被故意忽略** → 表现是"连上了、打字一点反应都没有、也没回显"），
点它一下就能收回来。

> **按钮变亮就是信号** —— 它亮起来 = "终端现在收不到键盘输入，点我"。
> 正常用的时候它**应该一直是灰的**；哪天发现它亮了，点一下就好。
>
> （它没有"进 Raw"的功能。Raw 是给程序用的，人没有理由主动进去。）

### REPL 波特率

右上角还有个 **REPL 波特率**，是**连接设备**时用的，和"烧写波特率"是**两回事**：

- 烧写波特率只在烧固件时用（esptool 自己定）
- REPL 波特率在连接板子时用，**必须和固件里设的一致**，否则会是乱码

**改完要重新连接才生效。** 不确定固件用多少，就用 `115200`。

---

## 五、文件管理

切到 **「文件管理」** 页。左边是电脑，右边是板子的文件系统。

| 按钮 | 作用 |
|---|---|
| **上传 →** | 左边选中的文件 → 传到板子当前目录 |
| **← 下载** | 右边选中的文件 → 下载到电脑当前目录 |
| **重命名 / 删除** | 对**板子**上的文件操作 |
| **刷新** | 重新读板子的目录 |

- **双击**左边的文件夹进去，双击 `../` 回上一级
- 两边的**路径框都能手改**，改完**按回车**生效（左边改电脑目录，右边改板子目录）
- 左边电脑的目录**开机就列好**，也会**记住你上次进到哪个目录**，下次打开还在
- 传 `.py` 文件到板子上：**复位板子后会自动运行**（叫 `main.py` 的话）

> ⚠ **板子上的文件用纯英文 / 数字最保险。** 中文经串口传可能丢字节，
> 文件会悄悄损坏。传完最好确认一下文件大小对不对。

### 板子不是 MicroPython 时，这一页用不了

**按钮会全变灰**，顶部会写清楚原因。

这不是工具没适配 —— **是那头根本没有可浏览的文件系统**。文件管理靠板子上的
MicroPython 提供文件接口（列目录 / 读写 / 改名 / 删除），裸机板（烧的是
Arduino、ESP-IDF 之类的程序）上只有一份烧进 flash 的二进制，没有"文件"这回事。

| 你的板子 | 文件管理 |
|---|---|
| 任何 ESP + MicroPython | ✅ 能用（和芯片型号无关）|
| 裸机 / Arduino / 别的固件 | ❌ 没有可管的东西 |

**烧写不受影响** —— 那个走的是芯片自带的 ROM，什么板子都能烧，去「烧写」页就行。

---

## 六、设置

### 「关于」

状态栏**右下角**有个「关于」按钮，点开能看到：

- **版本号和构建时间** —— 工具改得勤，看日期比看版本号实在
- 作者
- 支持哪些芯片
- 三个功能各自要什么前提（哪些需要板子上跑 MicroPython）
- 几个容易踩的点（复位 / 擦除 / Ctrl+C / 文件用 ASCII）

里面的字**可以选中复制**，方便贴给别人看。跟着语言设置走（中文界面显示中文，切英文就变英文）。

### 语言

顶部连接栏的 **语言** 下拉：**中文 / English**，随时切换，界面立刻变。
切换**不会丢**你正在看的东西 —— REPL 里的内容、固件列表、两边文件列表、
端口和波特率选择都会保留。

### 记住的偏好

关掉再打开，下面这些都还在（存在 `C:\Users\<你>\.btool.json`）：

- 上次选的**串口**（按端口名记，换 USB 口导致编号变了也能对上）
- **烧写波特率**、**REPL 波特率**
- **语言**
- **添加固件**时默认打开的目录
- **文件管理**里本地所在的目录

想恢复默认：**删掉 `C:\Users\<你>\.btool.json`** 再打开。

---

## 七、常见问题

| 现象 | 处理 |
|---|---|
| 串口列表空的 | 换根能传数据的 USB 线 / 装驱动 / 板子没供电 |
| "拒绝访问"打不开串口 | 别的程序占着 —— 关掉 ViperIDE、串口助手、另一个 bTool |
| 连上了但没反应、看不到 `>>>` | 按 `Ctrl+C` 打断板子上正在跑的程序 |
| 连上了、打字没反应 | 看右上角按钮是不是「切回 REPL」—— 是就点一下 |
| REPL 里全是乱码 | REPL 波特率和固件设的不一致，改成固件用的那个（不确定就 115200），**重新连接** |
| 上传的文件内容不对 | 文件名/内容用纯 ASCII；传完核对文件大小 |
| **烧写失败，报 `Serial data stream stopped`** | 见下 |
| 打包的 exe 烧写失败，但 `python btool.py` 正常 | 打包漏了 esptool 的数据文件，重新打包 |

### 烧写报 `Serial data stream stopped: Possible serial noise or corruption.`

**这不是波特率太高，也不是线不好。** 别去降波特率 —— 那只是把问题藏起来。

它来自 esptool 内部（`loader.py:2160`）：**已经成功收到过数据包，之后串口突然没声了**。
（真正的"握手失败"报的是另一句：`No serial data received`。）

原因是**提速的时机**。esptool 的官方流程是三段，顺序一步都不能错：

| 步 | 做什么 |
|---|---|
| ① | 用 **115200**（芯片 ROM 的默认速率）连接 |
| ② | 上 **stub flasher** 并让它跑起来 |
| ③ | **最后**才把波特率提到 921600 |

关键在 ②③ 的先后：**芯片 ROM 里的"改波特率"只改分频，不保证高速下还能稳定传数据**；
stub flasher 里的实现才是为高速设计的。在 ROM 阶段就提速，命令本身能成功
（所以芯片型号读得到、提速也报成功），但**紧接着开始传数据就断**。

工具已经按 ①②③ 排好了，所以**波特率可以放心用 921600**。
如果你是从很旧的版本升上来的，重新打包一次即可。

> 想自己核对：断开重来，看日志里 `已加载 stub flasher` 是不是排在
> `已提速到 921600 波特率` **前面**。
