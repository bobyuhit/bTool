# bTool 开发说明

> **这份是给「改这个工具的人」看的**：架构、实现要点、打包方法、踩过的坑。
>
> 用工具的人请看 → [README.md](README.md)（总览）· [用户手册.md](用户手册.md)（详细用法）

一个单文件的 Tkinter 小工具：选串口 → 选固件 → 点烧写，另外带上设备文件管理
和 REPL 终端。适用于全部 ESP 系列。

---

## 安装

```bash
pip install pyserial esptool
```

> esptool **必须 5.x**（本工具按 5.x 的 API 写的：`esptool.cmds.detect_chip` /
> `erase_flash` / `write_flash`，以及 `esptool.logger.EsptoolLogger` 进度钩子）。
> 4.x 的 API 完全不同，会直接报错。

## 运行

**打包好的**：直接双击 `dist/bTool.exe`，不用装 Python。

**从源码跑**：

```bash
python btool.py
```

---

## 功能说明

界面分三个选项卡，各自占满整个区域，互不干扰：

| 选项卡 | 干什么 |
|---|---|
| **烧写** | 选固件、擦除、烧写、看进度和 esptool 日志 |
| **REPL 终端** | 直接在里面敲命令，看板子输出 |
| **文件管理** | 本地 ↔ 设备 VFS 传文件、删除、重命名 |

### 0. 语言切换 / Language

顶部连接栏有 **语言** 下拉：**中文 / English**，随时切换，界面立即变。

切换时会**保留**当前状态 —— REPL 里的内容、已添加的固件列表、两边文件列表、
端口/芯片选择都不会丢。实现上是重建整个界面再恢复状态：Tkinter 没有全局重翻译的机制，
逐控件改文本要跟踪几十个控件，重建更省事也不容易漏。

> ⚠ **重建界面 = 所有列表控件都变空**，恢复状态时得逐个补回来。两个坑：
>
> - 状态栏文字**不**沿用旧的 —— 它存的是上一门语言的句子，直接恢复就会
>   「切成英文了状态栏还是中文」。所以按当前连接状态**重新生成**一条。
> - 本地文件列表**不在** `_save_state` 里（那里只存了设备侧的 `dev_rows`），
>   恢复时要 `refresh_local()` **重扫一遍磁盘**。漏了这一步，切完语言
>   文件管理页左边就空了 —— 而且只在**未连接**时出现（`refresh_local()` 早先
>   被缩进在 `if connected:` 分支里，连上设备正好顺手把它带跑了，所以更难发现）。

### 1. 烧写

| 操作 | 说明 |
|------|------|
| **添加固件…** | 可多选。地址会**按文件名自动猜**（见下表），双击某行可手改 |
| **双击地址列** | 手动改烧写地址（十六进制，如 `0x10000`）|
| **先擦除整片 Flash** | 默认**不勾**。勾上等同 `erase_flash`，见下方警告 |
| **擦除并烧写** | 开始。带进度条，下面是 esptool 的原始日志 |

**地址自动猜测规则：**

| 文件名包含 | 地址 |
|-----------|------|
| `bootloader` | `0x0` |
| `partition-table` / `partition_table` | `0x8000` |
| `boot_app0` | `0xE000` |
| 其它 | `0x10000` |

> ⚠ **擦除会清空整片 Flash —— 包括 NVS 里保存的标定 / 配置数据。**
> 很多固件把设备标定、网络配置、用户设置放在 NVS，擦了就全没了，得重新标定/配网。
> 只在改了分区表、或板子状态不明时才需要擦；平常更新程序**别勾**，直接烧。

**烧写时串口会被让出来**：esptool 要自己开串口，所以工具会先关掉 REPL 连接，
烧完自动重开（日志里会打 `[已释放串口供烧写使用]` / `[串口已重新打开]`）。

### 2. 文件管理

左边本地、右边设备 VFS。

| 按钮 | 作用 |
|------|------|
| **上传 →** | 本地选中 → 设备当前目录 |
| **← 下载** | 设备选中 → 本地当前目录 |
| **重命名 / 删除** | 对设备文件操作 |
| **刷新** | 重读设备目录 |

- 双击本地目录项进入，双击 `../` 回上级
- 左边本地列表**开机就填好**，不用先连设备、也不用点任何东西
- 设备文件走 **raw REPL**，传输用 base64 分块（避开引号转义和行缓冲限制）
- 可以传 `.py`：把脚本丢到板子上，复位即可运行

> 板上文件必须**纯 ASCII** 才保险 —— 蓝牙/串口传含中文的内容可能丢字节。
> 仓库里的 `mpy_modules/*.py` 就是按这个规则写的。

### 3. 记住偏好

关掉再打开，下面这些都还在（存在 `C:\Users\<你>\.btool.json`）：

| 记住什么 | 说明 |
|---|---|
| **添加固件的目录** | 下次点「添加固件…」直接落到上次那个目录 |
| 文件管理里的本地目录 | |
| 上次选的串口 | 按**端口名**匹配（换 USB 口导致编号变化时不强求）|
| 芯片、烧写波特率、REPL 波特率 | |
| 语言 | 下次打开就是上次那门语言 |

> 为什么存用户主目录而不是 exe 旁边：打包后的 exe 可能放在只读目录里跑。
> 写盘失败也不报错 —— 不该因为记个偏好把程序搞崩。
> 想重置：删掉 `C:\Users\<你>\.btool.json` 即可。

### 4. REPL 终端（独立选项卡）

**是终端，不是"输入框 + 输出框"** —— 直接在那个黑框里敲，就跟串口终端一样：

| 按键 | 发送 |
|------|------|
| 普通字符 | 原样发给板子 |
| `Enter` | `\r` |
| `Backspace` | `\x08` |
| `Tab` | `\t` |
| `↑` `↓` | `ESC[A` / `ESC[B`（翻历史）|
| `←` `→` | `ESC[D` / `ESC[C`（移动光标）|
| `Home` `End` | `ESC[H` / `ESC[F` |
| `Delete` | `ESC[3~` |
| `Ctrl+C` | **看有没有选中**：选中 → 复制；没选中 → `\x03\x03`（打断）|
| `Ctrl+Shift+C` / `Ctrl+Insert` | 复制（不分流，永远复制）|
| `Ctrl+V` / `Shift+Insert` | 剪贴板内容原样发给板子 |
| `Ctrl+D` | `\x04`（软复位 / raw 模式下的执行）|
| 右键 | 菜单：复制 / 粘贴 / 全选 / 清屏 |

> **⚠ `Ctrl+C` 必须分流，否则会误伤。** 终端里 `Ctrl+C` 的本义是打断（发 `0x03`），
> 但用户的肌肉记忆是"Ctrl+C = 复制" —— 想复制一段输出，顺手就把板子上跑的程序打断了。
> 按**有没有选中**分流（Windows Terminal 的做法）：
>
> ```
> 有选中 → 只复制, 不发字节
> 没选中 → 打断
> ```
>
> 一个键同时满足两个需求，且**谁都不会误触发另一个**。
> 早先的写法是 `bind("<Control-c>", lambda e: self.on_interrupt())`，两个问题：
> ① 无条件打断；② lambda 没 `return "break"`，所以 Tk 类级的 `<<Copy>>` 也会跟着跑，
> 变成"既复制又打断"—— 看起来能用，其实是两件事同时发生。

方向键**不是本地实现的**，是把这个 ANSI 序列发给板子、由 MicroPython 的 readline 处理
（历史记录深度 8 条）。板子按自己的方式回画 —— 翻历史是**整行重画**，左右移光标
只是**重印一个字符** —— 本工具负责把它回画的指令**渲染**出来，见下节。

- **屏幕上的字符是板子回显回来的**，不是本地画的。所以插了键盘之后要等板子回显才有反应 ——
  这是真终端的行为（好处是不会出现"本地显示的和板子实际收到的不是一个东西"）
- **切到 Raw / 切回 REPL** = 右上角那个按钮（文字跟着**实际**模式变）。
  文件操作用的是 raw 模式；raw 模式下键盘输入被忽略（那是程序化模式，不是给人敲的）
- 连接时**自动**打断并拿提示符，**不主动复位**板子（不会打断正在跑的程序）
- 右上角 **REPL 波特率**：连接设备用的波特率，**和"烧写波特率"是两回事**。
  两者可以不同（例：烧写用 921600 求快，REPL 用固件实际的 115200）。
  **改完要重新连接才生效**（连接时才会用到它）

---

## 打包成单文件 exe

```bash
pip install pyinstaller

# Windows (Git Bash 里必须带 MSYS_NO_PATHCONV=1, 否则 --add-data 的路径会被改写)
MSYS_NO_PATHCONV=1 python -m PyInstaller --onefile --windowed --name bTool \
  --icon=bTool.ico \
  --collect-submodules esptool \
  --add-data "$(python -c 'import esptool,os;print(os.path.dirname(os.path.dirname(esptool.__file__)))')/esptool/targets/stub_flasher;esptool/targets/stub_flasher" \
  --exclude-module IPython \
  --exclude-module matplotlib \
  --exclude-module PyQt5 \
  --exclude-module PyQt6 \
  --exclude-module PySide2 \
  --exclude-module PySide6 \
  --noconfirm --clean \
  btool.py
```

产物：`dist/bTool.exe`（单文件，双击即用，无需装 Python）。

**PowerShell 版本：**

```powershell
pyinstaller --onefile --windowed --name bTool `
  --icon=bTool.ico `
  --collect-submodules esptool `
  --add-data "$(python -c "import esptool,os;print(os.path.dirname(os.path.dirname(esptool.__file__)))")\esptool\targets\stub_flasher;esptool\targets\stub_flasher" `
  --exclude-module IPython --exclude-module matplotlib `
  --exclude-module PyQt5 --exclude-module PyQt6 --exclude-module PySide2 --exclude-module PySide6 `
  --noconfirm --clean `
  btool.py
```

### ⚠ 每个参数为什么不能省

**`--add-data ... stub_flasher`**

esptool 的 stub flasher 是一批 **JSON 数据文件**（`esptool/targets/stub_flasher/{1,2}/*.json`，
共 27 个），代码里按 `__file__` 的相对路径找：

```python
STUB_DIR = os.path.join(os.path.dirname(__file__), "targets", "stub_flasher")
```

PyInstaller **不会自动收集非 .py 文件**。少了它，exe **能启动、能连芯片，但一开始烧写就失败** ——
典型的"本机跑得好好的，打包出来就废"。所以必须把它按原路径结构塞进去。

**`--collect-submodules esptool`**

esptool 的芯片定义（`esptool/targets/esp32s3.py` 等 15 个）是**动态导入**的，
静态分析扫不到，不加会漏。

**`--exclude-module IPython / matplotlib / PyQt5 …`**

这条最容易被忽略，而且不加的话 exe 会**从几十 MB 膨胀到几百 MB**，打包时间也翻好几倍。

原因是一条很隐蔽的依赖链：

```
esptool/__init__.py → import rich_click as click     ← 无条件, 排不掉
        ↓
rich → (可选集成) pygments → (可选集成) IPython → matplotlib → PyQt5
```

`rich` 是 esptool 的**硬依赖**（`__init__.py:46` 直接 import，没有 try/except），所以不能排。
但从 rich 往下的那串全是 PyInstaller 的 hook 顺着**可选集成**摸出来的，运行时根本用不到 ——
本工具只用 esptool 的 `cmds` 和 `logger`，从不碰 CLI，也就用不到 rich 的渲染，
更用不到 IPython/matplotlib/Qt。所以把它们排掉。

**`--icon=bTool.ico`**

给 exe 一个图标。不写就是 PyInstaller 那个**羽毛笔**。

**`--windowed`**

不带控制台黑框。调试时**去掉这个参数**，esptool 的报错就能直接在终端看到。

### 应用图标

**exe 的图标和窗口的图标是两码事，得分别设** —— 这里最容易踩：

| 哪里的图标 | 怎么来 |
|---|---|
| **exe 文件**（资源管理器里看到的）| 打包时 `--icon=bTool.ico`，塞进 exe 的**资源段** |
| **窗口**（标题栏 / 任务栏）| 运行时 `set_window_icon()` 用 `iconphoto` 设 |

> ⚠ **exe 里的图标资源 Tk 读不到。** 只加 `--icon` 的话，资源管理器里图标是对的，
> 但**窗口上还是 Tk 那个默认的红色小方块**。所以运行时必须自己再设一次。

窗口图标用的是**内嵌在 `btool.py` 里的 base64 PNG**（`ICON_PNG_B64`），不读外部文件 ——
打包成单文件后外面那个 `.ico` 不一定会跟着走，内嵌才能保证 `btool.py`
仍然是"自包含的单个文件"。

**改图标**：编辑 `make_icon.py`（纯 Pillow 画的，不需要美术素材），然后

```bash
pip install pillow
python make_icon.py
```

它会产出 `bTool.ico`（多尺寸 16/24/32/48/64/128/256，让 Windows 按场景自己挑）
和 `_icon_preview.png`（各尺寸预览，含放大版 —— **重点看 16/24/32 糊不糊**），
最后把新的 base64 打印出来，贴回 `btool.py` 的 `ICON_PNG_B64` 即可。

> 画的时候按尺寸分了两档：`≤20px` 减少引脚数、放大芯片主体，
> 否则 16px 下细节会糊成一坨。改设计时别忘了这条。

### 打包后自检

```bash
# 1) 确认 stub 数据真的进去了 —— 直接在 exe 里找固件名
#    (onefile 模式下它是打进 exe 归档的, **不在 build/ 里**, 别去数 build 下的 json)
python -c "d=open('dist/bTool.exe','rb').read(); \
           print(all(n in d for n in (b'stub_flasher', b'esp32s3.json', b'esp32c3.json')))"

# 2) 直接跑起来, 用「添加固件 → 烧写」实烧一次
./dist/bTool.exe
```

第 2 步别省 —— stub 数据缺失是**只在真烧写时才暴露**的问题，光启动界面看不出来。

---

## 常见问题

| 现象 | 原因 / 处理 |
|------|------------|
| 串口列表为空 | 板子没插好、驱动没装（CH343/CP210x）、或被别的程序占着 |
| 打开串口报"拒绝访问" | 别的程序占着（ViperIDE / 串口助手 / 另一个 bTool）。关掉再试 |
| 烧写时 `Could not open port` | 同上 —— 烧写要独占串口 |
| 连上后没看到 `>>>` | 板子在跑 `while True` 程序，点 **Ctrl+C** 打断 |
| 进 raw REPL 失败 | 板上有外设会往串口吐数据时会干扰握手（例如某些板子的语音/蓝牙模块）。工具已自动重试 5 次，仍失败就多点几次「切到 Raw」|
| 上传后文件内容不对 | 检查文件是不是纯 ASCII；中文经串口/BLE 传输可能丢字节 |
| **烧写报 `Serial data stream stopped: Possible serial noise or corruption.`** | 见下节。**不要靠降波特率绕** |
| 打包的 exe 烧写失败但 python 跑正常 | 十有八九是 `--add-data stub_flasher` 漏了 |

### 关于 `Serial data stream stopped`

**不是波特率太高**，也不是串口有噪声。这个错的字面意思要看代码
（`loader.py:2160`）：

```python
read_bytes = port.read(...)
if read_bytes == b"":
    msg = ("Serial data stream stopped: ..." if successful_slip
           else "No serial data received.")
```

`successful_slip` 为真 = **已经完整收到过包**，之后串口哑了。所以它不是"握手失败"
（握手失败报的是 `No serial data received`），而是**中途断流**。

真因是**提速的时机**。esptool 的官方流程是三段，顺序一步都不能动
（`esptool/__init__.py`）：

```python
# 连接阶段: 不能超过 ROM 默认波特率
initial_baud = min(ESPLoader.ESP_ROM_BAUD, baud)      # :504
#                       # don't sync faster than the default baud rate
...
# 4) Upload the stub flasher
esp = run_stub(esp, plugins=...)                       # :592
# 5) Configure the baud rate
esp.change_baud(ctx.obj["baud"])                       # :600
```

| 步 | 做什么 | 做错了会怎样 |
|---|---|---|
| ① | 用 **115200**（ROM 默认）连接 | 拿 921600 去同步握手 → 握手就不稳 |
| ② | 上 **stub flasher** 并让它跑起来 | |
| ③ | **最后**才 `change_baud(921600)` | 在 ROM 阶段就提速 → 命令能 ACK，但紧接着传 stub 数据就断 |

**关键在 ②③ 的先后。** ROM bootloader 的 `CHANGE_BAUDRATE` **只改分频**，
不保证高速下还能稳定传数据；stub 的实现才是为高速设计的。

> 症状之所以迷惑：芯片型号**读得到**、提速也**报成功**，一开始传数据才断。
> 看起来像"链路质量不行"，其实是顺序错了 —— 所以**别靠降波特率绕**，
> 降到 115200 只是把问题藏起来。

`on_flash` 现在按 ①②③ 走，**波特率可以放心用 921600**。

---

## 实现要点（给接手的人）

**串口互斥**：三种用途争同一个串口 —— REPL、raw REPL、烧写。
前两个其实**是同一条连接的两种模式**（不用重开串口）；
只有烧写要真正独占，因为 esptool 自己开串口。所以：

```
烧写前: 关掉我们的连接 → esptool 独占 → 烧完重开
文件操作: 切到 raw REPL, 不重开串口
```

**⚠ 串口列表要定时重扫，但轮询不能碰状态栏**：插拔板子时列表得自己跟上 ——
新插的冒出来，**拔掉的消失**（不然下拉里会一直挂着已经不在的口）。
但 `var_status` 上还显示着"已连接 COM14"这类状态，如果轮询复用手动刷新那条路
（它结尾会 `set("找到 N 个端口")`），就会每 1.5 秒把连接状态顶掉。
所以拆成两个入口：

| 入口 | 更新列表 | 报状态栏 |
|---|---|---|
| `refresh_ports()`（刷新按钮）| ✅ | ✅ |
| `_auto_ports()`（定时器）| ✅ | ❌ |

选中项一律按**端口名**匹配（换 USB 口/重启后编号会变），优先级：
当前选中的 > 存档里的 > 第一个。
**当前选中的口拔掉了就空着，不跳到别的口上** —— 跳到别的口的话，板子插回来时
选择已经停在别处了，用户还得手动找回去。

**进度条怎么来的**：esptool 5.x 把日志收在一个单例 `EsptoolLogger` 里，
`set_logger()` 可以换掉它（实现上直接改单例的 `__class__`）。
子类化它、覆写 `progress_bar`，就把进度接进 Tkinter 了。

> ⚠ **坑：`EsptoolLogger.__new__` 是单例，子类会被它带偏。**
>
> ```python
> class BridgeLogger(EsptoolLogger): ...     # 直接继承
> EsptoolLogger().set_logger(BridgeLogger()) # ← 传进去的其实是那个单例本身!
> ```
>
> `BridgeLogger()` 走的是继承来的 `__new__`，返回 `cls.instance`（= 唯一的那个
> logger 实例）。于是 `set_logger` 里的 `self.__class__ = new_logger.__class__`
> 变成"赋成自己"——**静默空操作，还报成功**。症状：**烧写进度条不走、
> 烧写日志一片空白**，但功能"看起来正常"。
>
> 修法是给 `BridgeLogger` 覆写 `__new__`：`return object.__new__(cls)`，
> 拿一个真正的 BridgeLogger 实例。
>
> 判断有没有生效：`type(esptool.logger.log).__name__` 应该是 `BridgeLogger`，
> 不是 `EsptoolLogger`。

**终端怎么做到的**：一个 `tk.Text` + 一个**常驻读取线程**。
按键不本地插入，而是经 `on_term_key` 转发给串口；屏幕上看到的字符是板子回显的（见下）。
读取线程在连接期间一直跑，把设备输出实时 `post` 到 UI 队列。

**⚠ 文件操作后必须切回普通 REPL**：列目录 / 上传 / 下载 / 删除 / 重命名都要进 raw REPL，
**做完不切回来，终端就敲不动了** —— `on_term_key` 里有一道
`if sm.mode == "raw": return "break"`（raw 是程序化模式，键盘输入没意义）。
现象是**"连上了、打字毫无反应、也没回显"**。所以：

- **收在 `run_bg` 的 `finally` 里**，不在各操作自己末尾写。理由是**出错路径也得走到**：
  上传/下载失败时若停在 raw 模式，键盘输入被忽略 → 终端"哑"了，
  而那正是用户最需要敲字排查的时候。放在 `run_bg` 还顺带保证"新加的操作不可能忘"
- `_sync_mode_ui()` 让界面跟着**实际**模式走

**「切回 REPL」按钮只有"回来"这一个方向**，没有"手动进 raw"。
raw 是**给程序用**的模式，工具自己切进去、做完自己切出来，人没有理由主动进去
（键盘输入在里面本来就无效）。所以：

| 状态 | 按钮 |
|---|---|
| 未连接 / 已连接但模式正常 | **灰的**（日常状态，不占注意力）|
| 真卡在 raw | **可点** |

> **它亮起来本身就是信号**："终端现在收不到键盘输入，点我"。
> 实现上 `_sync_mode_ui()` 按 `sm.mode` 和 `sm.is_open` 设 `state`，**不再靠改按钮文字**
> —— 文字是固定的，状态用"能不能点"表达。
>
> 用户正常情况下**永远不该需要点它**：点了才通，就说明哪里漏了。

**⚠ 读取线程的循环条件不能带 `sm.is_open`**：烧写时会主动关串口让给 esptool，
那一刻 `is_open` 变 False；若把它写进 `while` 条件，线程会直接退出，
烧完重开串口后**终端再也不刷新**。只判 `_reader_on`，串口没开时跳过这一轮即可。

**⚠ 读取线程与请求-响应互斥**：`raw_exec` / `read_until` 这类一问一答的操作
必须独占读取，否则读线程会把响应抢走、那边就超时。所以 `SerialManager` 有个
可重入的 `exclusive()` 上下文管理器：

```python
with self.exclusive():      # 期间 sm.busy_io == True, 读线程让路
    ...
```

读线程每轮先看 `sm.busy_io`，为真就 `sleep` 跳过。**可重入**是必须的 ——
`raw_exec` 内部还会调 `enter_raw` → `read_until`，嵌套时不能互相解锁。
用 `finally` 释放，异常路径也不会把串口永久锁死。

**⚠⚠ `exclusive()` 需要两层，缺一不可**：

| 层 | 挡谁 | 实现 |
|---|---|---|
| `busy_io`（`_excl_depth > 0`）| **终端读线程** | 读线程每轮看它，为真就 sleep 跳过 |
| `_excl_serial`（`RLock`）| **别的工作线程** | 真正的互斥 |

**只做第一层是不够的 —— 这是个真踩过的坑。** 深度计数器只记录"嵌套了几层"，
它**不阻塞任何线程**。两个工作线程可以同时进 `exclusive()`，
一起往串口发 raw REPL 命令，把应答搅成一团，谁也别想拿到正确结果。

触发场景很隐蔽：`on_connect` 的工作线程 post 完 `connected` 之后**还继续干活**，
而 UI 线程收到 `connected` 就立刻 `refresh_device()` 又起了一个线程 ——
两边同时 `raw_exec`。**给连接流程加任何"连上后再做点什么"的功能都会撞上。**

`RLock` 正好同时满足两个要求：同一线程可重入，不同线程互斥。
判断有没有生效：4 个线程各进 30 次 `exclusive()` 并记录进出时刻，
**不应该有任何一次重叠**。

**⚠ 光有锁还不够 —— `enter_repl()` / `interrupt()` 也必须进来。**
这两个不是普通写入，是**改板子模式**的动作：

| 动作 | 字节 | 发在别人的 raw 交换中途会怎样 |
|---|---|---|
| `enter_repl()` | `\x02` | 板子**退出 raw REPL 并打开机横幅**，那边收到横幅而不是 `OK...` → `bad raw REPL response: b'\r\nMicroPython v3.0-...'` |
| `interrupt()` | `\x03` | 在 **raw REPL 里的含义是"清空当前行"** → 正在传的数据被打断 |

这两个都带 `acquire(timeout=2.0)` —— 它们会被 **UI 线程**调
（`back_to_repl` 和 Ctrl+C 按钮），**不能把 UI 冻住**。拿不到就先放弃：
真正在跑的那个操作收尾时还会再 post 一次 `back_to_repl`，能自愈。

> 这个错真出现过：连接线程探测完板子切回 REPL，正好撞上刷新线程在列目录，
> 弹 `bad raw REPL response` + 开机横幅。**同一个 `on_connect` 里既探测又
> 触发 `refresh_device()`，两条线天然并行** —— 加连接期的功能时必须想到这点。

**回显为什么要靠板子**：MicroPython 的友好 REPL 会原样回显输入字符。
如果按键时本地也 `insert` 一次、板子又回显一次，**每个字符会显示两遍**。
所以本地一律不插，只转发。代价是有回显延迟（板子收到才显示），
换来的是"屏幕上看到的就是板子上真实发生的"。

**⚠ 终端是个小型终端仿真器，不是"往后面追加日志"**：
板子的 readline 是**原地重画当前行**的（靠控制码挪光标），不是一行行往后打。
按"追加"处理，方向键就会坏掉 —— 而且坏的方式很迷惑。

| 按键 | 板子回什么 | 含义 |
|---|---|---|
| `←` | `\b` | **光标左移**（不删字符！）|
| `→` | **光标下的那个字符** | 重印一遍 = 右移一格（**不发 `ESC[C`**）|
| `↑` `↓` | `\b`×n + `ESC[K` + 新行 + `\b`×m | 重画本行（新行更长时**不擦行**）|
| `Backspace` | `\b` + `ESC[K` + 剩余文本 | 删一个字符 |

三个坑：

1. **裸 `\b` 是"移光标"，不是"删字符"。** 当成删除的话，按 `←` 会**吞掉最后一个字**
   （看起来像"左键没作用/删字"）。要删字符板子会**另发** `ESC[K`，所以 `\b` 绝不能删东西。
2. **`ESC[..` 是控制码，不能当文本插进去。** 否则按 `↑` 满屏乱码。
3. **打印是"覆盖"，不是"插入"。** 这是终端的基本语义：打印 = 光标处那个字符被替换掉，
   然后光标右移一格。板子的 readline 完全建立在这上面，两处都栽在这：

   | 场景 | 板子怎么做的 | 按"插入"处理会怎样 |
   |---|---|---|
   | `→` | `right_arrow_key` **不发 `ESC[C`**，只设 `redraw_step_forward = 1`，靠 redraw 把光标下的字符**重印一遍**（源码注释：`draw over old chars to move cursor forwards`）| 字符被**复制一份**：`>>> abc` → `>>> abcc` |
   | `↑` 翻历史 | 只有新行**比旧行短**才擦行（`if line->len < last_line_len`）；新行更长时**不擦，直接重印覆盖** | 旧行残留跟新行**拼在一起**：`>>> abc` → `>>> abcabcdef` |

光标就是 `tk.Text` 的 **insert mark** —— 一切输出都画在 `"insert"` 处，不再往 `"end"` 追加。
`_term_putc()` 负责"打印一个字符"（必要时先 `delete` 掉光标处那个字符再写）。
`_term_csi()` 只认板子真会发的那几条（`K` 擦行 / `C` `D` 移光标 / `H` `F`），**其余一律丢弃**。

> 依据：`shared/readline/readline.c` 的 `mp_hal_move_cursor_back()` ——
> 后退 ≤4 步走 `\b` 快路径（`"\b\b\b\b"` 取前 n 个），更多才发 `ESC[<n>D`；
> 擦行统一发 `ESC[K`。

**转义序列会被串口 `read` 切成两块**（`ESC[` 和 `3~` 分两次到）。
所以 `_term_esc` 要**跨调用**暂存半截序列，收全了再执行 —— 切开的半截绝不能当文本渲染。

**其它控制字符**：`\r\n` / 孤立 `\r` 统一成换行；`\x07`(响铃) 丢掉；其余控制字符丢弃。

**往 `end` 插入不会搬动 insert mark** —— 语言切换恢复终端内容（`_restore_state`）时
必须显式 `mark_set("insert", "end-1c")`，否则光标滞留在 1.0、后续输出画错地方。

**⚠ 粘贴的两个坑（都跟换行有关）**：

| 坑 | 表现 | 原因 |
|---|---|---|
| 换行发的是 `\n` | 多行**并成一行** → `SyntaxError` | 板子的 readline 里**只有 `c == '\r'` 是回车执行**（`readline.c:195`），**没有 `\n` 分支** —— `\n` 掉到默认分支被当**普通字符**插进当前行（`vstr_ins_char`）。剪贴板给的换行正是 `\n` |
| 多行逐行发 | `for`/`if` 报缩进错 | 板子的自动缩进开着（`MICROPY_REPL_AUTO_INDENT`，esp32 的 ROM 级别够），带缩进的行会被**再加一层**；而且复合语句还要**再补一个空行**才算输入完 |

所以粘贴分三种走法：

| 粘的内容 | 怎么发 | 粘完的状态 | 进历史 |
|---|---|---|---|
| **单行** | 原样发，**末尾不加 `\r`** | 停在命令行等回车 | — |
| **多行·简单语句** | 逐行发，**只有最后一行不加 `\r`** | 前几行已执行，**最后一行等回车** | ✅ **进** |
| **多行·带缩进/复合语句** | paste 模式（`\x05` … `\x04`）| 直接执行 | ❌ 不进 |

**最后一行不补 `\r`** 是刻意的：粘贴等于"帮你打字"，执行与否由用户按回车决定，
好让他先看一眼。**前面的行必须补 `\r`** —— 不补的话它们会跟最后一行黏成一行。

> 复合语句那条路做不到"留最后一行"：把 `for i in range(3):` 单独执行会直接
> SyntaxError（循环体不在）。所以整段一起走 paste 模式。

**多行分两条路**，判据见 `_paste_by_lines_ok()`：逐行发的好处是**每行都进板子历史**
（`\r` 分支里就是 `readline_push_history`），↑ 能翻出来；但板子的自动缩进
（`readline_auto_indent`）会按上一行给新行补空格 ——

```c
int n = (j - i) / 4;                          // 上一行的前导空格数
if (line->buf[line->len - 2] == ':') n += 1;  // 上一行以冒号结尾
while (n-- > 0) { 补 4 个空格 }
```

所以这种块逐行发会**悄悄改语义**：

```python
if x:
    a = 1
b = 2        # ← 自动缩进沿用上一行的 8 空格, 被吸进 if 块里
```

因此**只有自动缩进保证一次都不插手**时才逐行发：没有行以空白开头、没有行以 `:` 结尾、
没有行以复合语句关键字开头（`if`/`for`/`def`… 板子会判成"没输完"而卡在续行）。
其余一律走 paste 模式 —— 宁可翻不到历史，不能改语义。

> **多行·复合语句**走 paste 模式：发 `Ctrl-E`(`\x05`) 进、灌原文、再发 `Ctrl-D`(`\x04`) 执行。

```python
self.term_send(b"\x05")          # Ctrl-E: 板子回 "paste mode; Ctrl-C to cancel, Ctrl-D to finish"
... send raw text ...            # 期间板子**原样收进缓冲区**, 不做自动缩进、不逐行执行
self.term_send(b"\x04")          # Ctrl-D: 把整段**当文件**一次编译执行 (pyexec.c:368)
```

> **paste 模式里发 `\n` 而不是 `\r`**：那个模式下 `\r` 只是"换行 + 报个 `=== `"，
> 不执行（执行靠 Ctrl-D）；而收到的字节会**原样**进缓冲区当源码，源码行尾本就该是 `\n`。
>
> 大段粘贴要**分块 + 小睡**：板子的串口接收缓冲不大，一口气灌进去会**丢字节** ——
> 丢字节是"悄悄改掉你几个字"，报个看不懂的错，最难查。

**芯片信息走 ROM，不走 REPL**：

> 这条是本项目的**定位决定**的：工具要针对**所有 ESP 芯片**，不能建立在
> "板子上跑着 MicroPython" 这个前提上。ROM bootloader 是**出厂烧在硅片里的**，
> 刷什么固件都盖不掉 —— 裸机板、MicroPython、出厂空白，一样读得出来。

| | ROM（用这个）| REPL（不用）|
|---|---|---|
| 板上要跑什么 | **什么都不用** | 必须 MicroPython |
| 拿得到 | 型号、**修订版**、封装、**MAC**、晶振、Wi-Fi/BT 特性、分区表 | 固件版本、内存、文件系统 |
| 代价 | **芯片要复位进下载模式**（跑着的程序会重启）| 无 |

**两个时机**，都是"本来就要进下载模式"的场合，所以不额外打扰：

| 时机 | 触发 | 代码 |
|---|---|---|
| **烧写时** | 自动 | `on_flash` 里 `detect_chip` 之后 |
| **读芯片信息** | **用户点按钮** | `on_read_chip` |

> ⚠ **绝不能做成"连接时自动读"** —— 那等于每次连板子都把它上面的程序重启一遍。
> 按钮点之前还会 `askyesno` 问一句，因为这事会重启板子。

`chip_detail_lines(esp)` 每个字段单独 try —— 不同芯片/ROM 能给的字段不一样，
拿不到就跳过；一个字段失败不能把整块信息搞没。排版对齐 esptool 命令行。

**顺便读分区表**：`read_partitions_rom(esp)` 从 flash `0x8000` 读，每项 32 字节：

```
magic(2)=0x50AA   type(1)  subtype(1)  offset(4)  size(4)  label(16)  flags(4)
```

读到 magic 对不上为止（表尾是 `0xFF` 填充）。

> 这是**在电脑这边解析**的（esptool 把原始字节读回来）—— 所以和板子上跑什么无关。
> ⚠ 没上 stub 时 esptool 走 `read_flash_slow`，3KB 要好几秒；
> 调用方（`on_flash` / `on_read_chip`）都先 `run_stub()` 再读。

**⚠ 文件管理只在板子跑 MicroPython 时可用**，别的芯片/固件一概不适用 ——
它走的是 `os.listdir` / `open` / `os.rename` / `os.remove` 这套 API。

> **这是固有边界，不是"没适配好"**：裸机板上只有一份烧进 flash 的二进制，
> 根本没有可浏览的文件系统。所以**不假装能干，而是说清楚为什么不能干**。

判据是**现成的，不用额外探测**：连接时有没有拿到 `>>>` 提示符
（`on_connect` 本来就在等它）。拿不到 → 板子不是 MicroPython →
`_sync_file_ui()` 把文件页按钮全置灰 + 顶部说明原因，`refresh_device()`
也直接返回、不发命令。

> 之前的表现是：点了弹一堆 raw REPL 超时，用户完全不知道发生了什么。
>
> `post("repl_ok")` 必须**排在 `post("connected")` 前面** —— 后者会触发
> `_after_connect`，而它里面就要用这个值了。

**raw REPL 协议**（**文件管理用**，芯片信息不走这条）：
- 发 `\x01` 进 raw 模式 → 回 `raw REPL; CTRL-B to exit`
- 发 `代码 + \x04` → 回 `OK<stdout>\x04<stderr>\x04>`
- 解析时**从第一个 `OK` 开始**，避开回显和杂讯

**中英文切换**：所有界面字符串抽在文件顶部的 `LANG` 字典里（`zh` / `en` 两份），
代码里一律用 `tr("key")` 取。加新文案时**两边都要加**，漏了会显示成 key 本身。
切换时重建界面 + 恢复状态（见 `_save_state` / `_restore_state`）。

有个坑：状态栏**不能**沿用旧文字。它存的是上一门语言的句子，直接恢复就会
"切成英文了状态栏还是中文"。所以 `_restore_state` 里按当前连接状态**重新生成**一条。

**文件传输**：base64 分块。每块长度必须是 4 的倍数，否则单块无法独立解码。
读用 hex（2 倍体积但绝对无歧义），写用 base64。

**线程模型**：所有串口 I/O 在后台线程，UI 更新走 `queue` + `after(60, ...)` 轮询。
Tkinter 不是线程安全的，后台线程绝不直接碰控件。

**⚠ 关窗要取消两个定时器**：`_port_timer`（串口轮询）和 `_drain_id`（UI 消息泵）。
`_drain` 每轮结尾会 `self.after(60, self._drain)` **重排自己** —— 不显式取消，
控件销毁后它还会触发一次，Tcl 直接报 `invalid command name "..._drain"`。
打包成 `--windowed` 后没有控制台，这个报错**完全看不见**，所以别指望靠观察发现。
